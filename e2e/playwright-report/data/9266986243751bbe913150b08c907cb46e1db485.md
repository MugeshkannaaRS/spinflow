# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: dashboard.spec.ts >> Dashboard >> loads without crash and shows KPI cards
- Location: tests/dashboard.spec.ts:10:7

# Error details

```
Test timeout of 120000ms exceeded while running "beforeEach" hook.
```

```
Tearing down "context" exceeded the test timeout of 120000ms.
```

```
TimeoutError: page.fill: Timeout 30000ms exceeded.
Call log:
  - waiting for locator('input[name="email"]')

```

# Test source

```ts
  1  | import { Page, expect } from '@playwright/test';
  2  | import users from '../fixtures/users';
  3  | 
  4  | export async function loginAs(page: Page, role: keyof typeof users) {
  5  |   const user = users[role];
  6  |   await page.goto('/');
> 7  |   await page.fill('input[name="email"]', user.email);
     |              ^ TimeoutError: page.fill: Timeout 30000ms exceeded.
  8  |   await page.fill('input[name="password"]', user.password);
  9  |   await Promise.all([
  10 |     page.waitForNavigation({ url: '**/dashboard', timeout: 10000 }).catch(() => {}),
  11 |     page.click('button[type="submit"]')
  12 |   ]);
  13 | }
  14 | 
  15 | export async function expectKpiCards(page: Page) {
  16 |   // generic check: at least one KPI card exists and contains a number or 'kg' or '₹'
  17 |   const cards = page.locator('.kpi-card, .kpi', { hasText: /\d|kg|₹/ });
  18 |   await expect(cards.first()).toBeVisible();
  19 | }
  20 | 
```