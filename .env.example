# SpinFlow ERP — Environment Variables

# Backend API URL (set this to connect to your FastAPI backend)
VITE_API_BASE_URL=http://localhost:8000

# When VITE_API_BASE_URL is not set, the app uses the mock API
# for development and demo purposes
